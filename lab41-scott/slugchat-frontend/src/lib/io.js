import io from 'socket.io-client';

// export const socket = io(`${__API_URL__}`);

//socketio finds the event it listens to from the backend.
// the message or data is passed through.
// socket.on('the event', (data) => {
//   console.log('FE event data: ', data);
// });
